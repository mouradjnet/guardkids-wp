# Dependências Node (build do PWA — Fase M2)
/node_modules/

# IDE / editores
/.idea/
/.vscode/

# Logs e caches
*.log
/.phpunit.result.cache
/.phpunit.cache/

# Ambiente de testes wp-env (Docker)
/.wp-env-home/

# Sistema operacional
.DS_Store
Thumbs.db

# Dependências PHP de desenvolvimento — o Composer só é usado em dev
# (PHPUnit); o plugin não tem dependências de runtime.
/vendor/

# Variáveis de ambiente locais (NUNCA commitar — contêm Application Passwords).
# .env.example fica versionado como template.
.env
.env.local
.env.*.local
