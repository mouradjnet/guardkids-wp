# Upstream do nginx do site no LocalWP. Mesma porta usada pelo app-parent
# (visivel em LocalWP -> Site -> Advanced). Vite proxia /wp-json/* pra esse alvo.
VITE_WP_PROXY_TARGET=http://127.0.0.1:10034
